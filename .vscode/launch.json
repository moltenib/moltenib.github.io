{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open index.html",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "file://${workspaceFolder}/index.html",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
